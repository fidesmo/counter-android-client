<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">


    <Button
        android:id="@+id/readCounterButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/read_counter_button"
        android:layout_centerHorizontal="true"
        android:textSize="@dimen/abc_text_size_large_material" />

    <Button
        android:id="@+id/decrementCounterButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/decrease_counter_button"
        android:layout_below="@+id/readCounterButton"
        android:layout_centerHorizontal="true"
        android:textSize="@dimen/abc_text_size_large_material" />

    <TextView
        android:id="@+id/counterValue"
        android:text="@string/default_counter_value"
        style="@style/Base.TextAppearance.AppCompat.Medium"
        android:layout_below="@+id/decrementCounterButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:textAlignment="center"
        android:layout_centerHorizontal="true"
        android:textSize="40dp" />

    <TextView
        android:id="@+id/main_text"
        android:text=""
        style="@style/Base.TextAppearance.AppCompat.Medium"
        android:layout_below="@+id/counterValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>



</RelativeLayout>
